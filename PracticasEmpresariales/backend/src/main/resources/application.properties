# ================================================================
# PRACTICAS EMPRESARIALES CUE - Configuracion Principal
# ================================================================

spring.profiles.active=mysql

# ----------------------------------------------------------------
# SERVIDOR
# ----------------------------------------------------------------
server.port=8080
server.servlet.context-path=/api

# ----------------------------------------------------------------
# JPA / HIBERNATE
# ----------------------------------------------------------------
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=false

# ----------------------------------------------------------------
# JWT
# ----------------------------------------------------------------
app.jwt.secret=cue-practicas-empresariales-clave-secreta-alexander-von-humboldt-2026-cambiar-en-produccion
app.jwt.expiration-ms=86400000

# ----------------------------------------------------------------
# CORREO
# ----------------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=maherrera_115@cue.edu.co
spring.mail.password=aaitrrhmjwgxvobc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8
spring.h2.console.enabled=false

app.mail.from.name=Sistema Practicas CUE
app.mail.from.address=maherrera_115@cue.edu.co

# ----------------------------------------------------------------
# CORS
# ----------------------------------------------------------------
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# ----------------------------------------------------------------
# MULTIPART / UPLOADS
# ----------------------------------------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=11MB

# ----------------------------------------------------------------
# JACKSON — fechas como ISO strings (no arrays)
# ----------------------------------------------------------------
spring.jackson.serialization.write-dates-as-timestamps=false

# ----------------------------------------------------------------
# SISTEMA
# ----------------------------------------------------------------
app.nombre=Sistema de Gestion de Practicas Empresariales
app.universidad=Universidad Alexander Von Humboldt

# ----------------------------------------------------------------
# USUARIO ADMINISTRADOR INICIAL
# ----------------------------------------------------------------
app.init.admin.correo=dti@cue.edu.co
app.init.admin.password=Admin2026!
app.init.crear.coordinador.test=false
app.init.coordinador.test.password=

# ----------------------------------------------------------------
# TESTING (Postman / QA) — NUNCA activar en producción
# Si es true, /auth/login devuelve el código 2FA en claro (campo
# "codigoDebug") para poder automatizar el login sin revisar el correo.
# Activar solo localmente con: -Dapp.test.expose-codigo-2fa=true
# ----------------------------------------------------------------
app.test.expose-codigo-2fa=false
